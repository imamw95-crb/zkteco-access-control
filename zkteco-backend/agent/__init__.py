"""Windows push agent for ZKTeco C3 panels.

Kept importable as a package so `encode_records` can be unit-tested on a machine
without the 32-bit Pull SDK DLL; the script still runs standalone
(`python zk_push_agent.py`).
"""
